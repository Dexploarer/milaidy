## 2025-04-05 - ARIA Labels on Icon Buttons with Titles
**Learning:** Icon-only interactive elements require an explicit `aria-label` for screen reader accessibility, even if a visual `title` attribute is already present.
**Action:** Always add `aria-label` to icon-only buttons (e.g., delete buttons using '×') alongside `title` to ensure proper vocalization context.
