## 2025-02-18 - [ConversationsSidebar Delete Button]
**Learning:** Hidden actions (opacity-0) in lists must have `focus:opacity-100` to be accessible via keyboard.
**Action:** Always add `focus:opacity-100` when using `group-hover:opacity-100` for action buttons. Ensure icon-only buttons have `aria-label`.
