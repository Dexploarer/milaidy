## 2026-02-24 - Accessibility Gaps in Interactive Lists
**Learning:** Icon-only buttons (like delete 'x') in list items are a common accessibility gap, missing ARIA labels and visual affordance.
**Action:** Always pair icon-only buttons with `aria-label` and consider SVG icons over text characters for better click targets and visual clarity.
